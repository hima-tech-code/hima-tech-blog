import { Tag } from 'tag'

model Post {
  postId      Int      @id @default(autoincrement())
  titile      String
  description String
  body        String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  tags        Tag[]
}
